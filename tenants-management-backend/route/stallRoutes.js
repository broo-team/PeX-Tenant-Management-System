// const express = require('express');
// const stallController = require('../controller/stallController.js');

// const router = express.Router();

// router.post('/stalls', stallController.createStall); // Create stall with only stallCode
// router.post('/stalls/:stallCode/details', stallController.addStallDetails); // Add details to stall
// router.get('/stalls', stallController.getStalls);
// router.put('/stalls/:stallCode', stallController.updateStall);
// router.delete('/stalls/:stallCode', stallController.deleteStall);

// module.exports = router;